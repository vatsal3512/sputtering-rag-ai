# ── App ───────────────────────────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.20
pandas>=2.0
numpy>=1.26

# ── LangChain ─────────────────────────────────────────────────────────────────
langchain-core>=0.3            # prompts, output parsers, messages (langchain_core.*)
langchain-google-genai>=2.0
langchain-community>=0.3
langgraph>=0.2

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=0.5
sentence-transformers>=3.0       # SciBERT local embeddings — no API key needed

# ── Gemini (direct SDK — used by pipeline scripts) ────────────────────────────
google-generativeai>=0.8
google-api-core>=2.19            # provides ResourceExhausted exception

# ── Pipeline ──────────────────────────────────────────────────────────────────
beautifulsoup4>=4.12             # Stage 1: parse GROBID TEI XML
lxml>=5.2                        # Stage 1: required by BeautifulSoup xml parser
matplotlib>=3.8                  # Stage 3: EDA histogram plots

# ── Validation ────────────────────────────────────────────────────────────────
pydantic>=2.0                    # Stage 2: validate LLM JSON output schema

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv>=1.0               # load API keys from .env file